<%= form_for(@dell) do |f| %>
  <% if @dell.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dell.errors.count, "error") %> prohibited this dell from being saved:</h2>

      <ul>
      <% @dell.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first %><br />
    <%= f.text_field :first %>
  </div>
  <div class="field">
    <%= f.label :second %><br />
    <%= f.text_field :second %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
